#include "main.h"

/**
 * add - adds two number and returns the result
 * @x: first number
 * @y: second number
 *
 * Return: returns the result of sum of two numbers
 */

int add(int x, int y)
{
	return (x + y);
}
